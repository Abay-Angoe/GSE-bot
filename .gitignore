# Secrets
.env

# Databases (gse.db/africa.db) are committed as a data backup — kept in the repo.
# Runtime state stays local.
scheduler_state.json

# Logs
*.log

# Python / envs
__pycache__/
*.pyc
.venv/
venv/
